@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/SmaxKernelsTargets.cmake")

# Expose variables to consumers
set(SMAX_CUDA_MODE_EXPORT @SMAX_CUDA_MODE@)
set(SMAX_LIKWID_MODE_EXPORT @SMAX_LIKWID_MODE@)

# Optionally, you can add some messages or usage notes here
message(STATUS "SmaxKernels found with SMAX_CUDA_MODE_EXPORT=${SMAX_CUDA_MODE_EXPORT} and SMAX_LIKWID_MODE_EXPORT=${SMAX_LIKWID_MODE_EXPORT}")

# Locate the target library in the install prefix
include("${CMAKE_CURRENT_LIST_DIR}/SmaxKernelsTargets.cmake")
